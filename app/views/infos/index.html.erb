<nav class="navbar navbar-expand-lg bg-body-tertiary">
  <div class="container-fluid">
    <a class="navbar-brand" href="#">Wallboard</a>
    <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
      <span class="navbar-toggler-icon"></span>
    </button>
    <div class="collapse navbar-collapse" id="navbarSupportedContent">
      <ul class="navbar-nav me-auto mb-2 mb-lg-0">
        <li class="nav-item">
          <a class="nav-link" href="boards">Sales Board</a>
        </li>
    </div>
  </div>
</nav>
  <div class="container text-center" id="header">
  </div>
<div>
<h3>Note: if all values are the same as another record the record will not be imported</h3>
<h3>Note: this test app has the ability to import info from the csv and add it to the db then display a total for the selected fields</h3>
<div>
<%= form_tag import_infos_path, method: :post, multipart: true do %>
  <%= file_field_tag :file, accept: ".csv" %>
  <%= submit_tag '⚙️ Import' %>
<% end %>
</div>
</div>
<p style="color: green"><%= notice %></p>

<h3>Infos</h3>
  <table>
   <thead>
<tr>
   <th><strong>Date</strong></th>
   <th> <strong>Ord</strong>  </th>
   <th> <strong>Val</strong></th>
   <th> <strong>Bud Ord</strong></th>
   <th> <strong>Bud Val</strong></th>
   <th> <strong>PY Ord</strong></th>
   <th> <strong>PY Val</strong></th>
 </tr>
</thead>
<tbody>
<div id="infos">
  <% @infos.each do |info| %>
    <tr><%= render info %></tr>
  <% end %>
</div>
</tbody>
</table>

<div>
<%= link_to "New info", new_info_path %>
</div>





